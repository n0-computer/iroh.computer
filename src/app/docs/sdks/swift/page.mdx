import { SdkResources } from '@/components/SdkResources';
import { CodeBracketIcon } from '@heroicons/react/20/solid';

export const metadata = {
  title: 'Iroh Swift',
  description:
    'Iroh swift library',
};

# Iroh in Swift

Build native iOS apps with iroh\'s swift SDK, tested with SwiftUI, works with uikit. {{ className: 'lead' }}

{/* <SdkResources
  language='swift'
  githubUrl='https://github.com/n0-computer/iroh-ffi'
  resources={[
    {
      href: '/docs/examples/ios-starter',
      name: `iOS Starter`,
      description: `quickstart for building with iroh on iOS`,
      icon: CodeBracketIcon,
      pattern: {
        y: 32,
        squares: [
          [0, 2],
          [1, 4],
        ],
      },
    }
  ]}
/> */}